const differentIdsValidation = require('./differentIdsValidation');
const fieldsValidation = require('./fieldsValidation');
const JWTValidation = require('./JWTValidation');

module.exports = {
  differentIdsValidation,
  fieldsValidation,
  JWTValidation,
};
